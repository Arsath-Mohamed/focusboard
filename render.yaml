services:
  - type: web
    name: focusboard-v12
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    autoDeploy: false
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: APP_NAME
        value: FocusBoard
